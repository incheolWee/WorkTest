package com.example.worktest.work;

import java.util.List;

public interface WorkService {
    Work createWork(WorkDTO workDTO);
//    List<Work> getWorksByUser(Long userId);
//    Work deleteWork(Long workId);
}
